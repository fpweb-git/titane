Running the Editor (Development)
To launch the editor interface with Hot Module Replacement (HMR) enabled:

The editor will be available at http://localhost:3000.

🛠️ Useful Commands
npm run core:dev : Compiles the core engine in watch mode (useful if you are developing the core logic independently).